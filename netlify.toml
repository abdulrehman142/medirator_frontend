[build]
  # No build step required — publishing prebuilt `dist` dropped into repo root
  command = "echo 'Using prebuilt dist; no build required'"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
